# Runtime execution toggle (1 to enable runtime execution)
THINKING_OS_RUNTIME_EXEC=1

# Optional directory for local model storage (defaults to ~/LOCAL-AI-MODELS)
MODELS_DIR=

# Optional Hugging Face access token for gated models
HF_TOKEN=
